import { render } from '@testing-library/react';
import Router from './Router';

describe('Router | component | integration tests', () => {
  xit('renders pages respectively to current URL path', () => {
    render(<Router />);
  });
});
