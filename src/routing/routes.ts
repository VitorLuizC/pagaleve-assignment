export const ROOT_PATH = '/';

export const CONTACTS_PATH = '/contacts';

export const CONTACTS_CREATE_PATH = `${CONTACTS_PATH}/create`;

export const CONTACTS_UPDATE_PATH = `${CONTACTS_PATH}/:contactId`;
